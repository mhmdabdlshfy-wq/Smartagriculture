{
    "name": "smart-agri-mern",
    "version": "1.0.0",
    "description": "Smart Agriculture Monitoring System - MERN Stack",
    "scripts": {
        "start": "node back/server.js",
        "build": "npm --prefix back install && npm --prefix front install && npm --prefix front run build",
        "install-all": "npm --prefix back install && npm --prefix front install",
        "dev": "node back/server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}